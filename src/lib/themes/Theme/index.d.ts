import { ReactNode } from 'react';
export interface ThemeProps {
    children: ReactNode;
}
export declare function Theme({ children }: ThemeProps): import("@emotion/react/jsx-runtime").JSX.Element;
export default Theme;
